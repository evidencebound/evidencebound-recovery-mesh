# Proof-of-Action capture shot list

1. Live verified baseline — `google_adk_vertex`, Gemini 3.5 Flash, four ADK roles, all checkpoints verified.
2. Controlled stale-evidence trust break — red source, exact blast radius, blocked `publish_action`, Scout reused.
3. Verified selective recovery — affected branch recomputed, action resumed only after deterministic verification.
4. Measured benchmark panel — use the actual fresh run values shown by the UI; reference run `run-4707af5a2fb6` is separate evidence.
5. Google control-plane proof — Cloud Run revision, Agent Registry receipt, Secret Manager name/version only, WIF/GitHub Actions receipt.

No secret value, request header, or private Devpost judge key may appear in the public video.
